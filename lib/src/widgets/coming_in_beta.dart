import 'package:flutter/cupertino.dart';

class ComingInBeta extends StatelessWidget {
const ComingInBeta({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Center(child: 
    Text("Beta coming soon! Visit our website for the latest updates."),);
  }
}