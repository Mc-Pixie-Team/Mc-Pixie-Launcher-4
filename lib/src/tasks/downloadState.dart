enum DownloadState {
  notDownloaded,
  downloadingClient,
  downloadingLibraries,
  downloadAssets,
}

enum ForgeInstallState {
  downloadingClient,
  downloadingLibraries,
  downloadAssets,
  patching,
  finished,
}

enum ClientInstallState {
  downloadingClient,
  downloadingLibraries,
  downloadAssets,
  finished,
}

